# TwoSum
1. sum_two(): given an integer array and target integer, the function checks if there exists sum of two elements of the array equals the target number
 
2. sum_two2(): given an integer vector and target integer, the function checks if there exists sum of two elements of the vector equals the target number
